# robots.txt for theperfectsmileclinic.com
# Lives in /public/ so the build copies it to the site root.
# Apex (non-www) is the canonical host. Both apex and www serve the
# site; the sitemap and canonical links advertise the apex form only.
# Verify after deploy: https://theperfectsmileclinic.com/robots.txt

User-agent: *
Allow: /

# Admin shell + login both live under /adminauthlogin — keep the prefix
# off the index so Google doesn't surface a clinic-staff login to patients.
Disallow: /adminauthlogin

Sitemap: https://theperfectsmileclinic.com/sitemap.xml
